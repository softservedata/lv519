package academy.softserve;

public class Fails {
/*
    private Map setRegistrationVariables(String mail, String name, String password, String confirmPassword) {
        String field = "email:" + mail + "username:" + name + "password:" + password + "passwordConfirm:" + confirmPassword;
        Map<String, String> fieldsValue = Arrays.stream(field.split(","))
                .map(s -> s.split(","))
                .collect(Collectors.toMap(s -> s[0], s -> s[1]));
        return fieldsValue;
    }

    private Map setExpectedErrors(String mail, String name, String password, String confirmPassword) {
        String field = "email:" + mail + "username:" + name + "password:" + password + "passwordConfirm:" + confirmPassword;
        Map<String, String> fieldsValue = Arrays.stream(field.split(","))
                .map(s -> s.split(","))
                .collect(Collectors.toMap(s -> s[0], s -> s[1]));
        return fieldsValue;
    }

    private Map<String, String> setDaa(String mail, String name, String password, String confirmPassword) {
        Map<String, String> fieldsValueSet = new HashMap<>();
        fieldsValueSet.put("mail", mail);
        fieldsValueSet.put("username", name);
        fieldsValueSet.put("password", password);
        fieldsValueSet.put("passwordConfirm", confirmPassword);
        return fieldsValueSet;
    }*/
}
